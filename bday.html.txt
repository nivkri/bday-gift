<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">

<title>Happy Birthday ❤️</title>

<style>
* {
  box-sizing: border-box;
  margin: 0;
  padding: 0;
}

html {
  scroll-behavior: smooth;
}

body {
  min-height: 100vh;
  font-family: "Segoe UI", Arial, sans-serif;
  color: white;
  background:
    radial-gradient(circle at 50% 20%, #681b59 0%, #260a2d 35%, #0b0012 100%);
  overflow-x: hidden;
}

/* =========================
   BACKGROUND
========================= */

.stars,
.hearts,
.sparkles {
  position: fixed;
  inset: 0;
  pointer-events: none;
  z-index: 0;
}

.star {
  position: absolute;
  width: 3px;
  height: 3px;
  background: white;
  border-radius: 50%;
  animation: twinkle 3s infinite ease-in-out;
}

@keyframes twinkle {
  0%, 100% {
    opacity: .2;
    transform: scale(.7);
  }

  50% {
    opacity: 1;
    transform: scale(1.5);
  }
}

.floating-heart {
  position: fixed;
  bottom: -60px;
  font-size: 25px;
  opacity: .7;
  animation: floatHeart linear infinite;
  z-index: 1;
}

@keyframes floatHeart {
  0% {
    transform: translateY(0) rotate(0deg);
    opacity: 0;
  }

  10% {
    opacity: .8;
  }

  100% {
    transform: translateY(-115vh) rotate(360deg);
    opacity: 0;
  }
}

/* =========================
   HERO
========================= */

.hero {
  min-height: 100vh;
  display: flex;
  justify-content: center;
  align-items: center;
  text-align: center;
  position: relative;
  padding: 30px 20px;
  z-index: 2;
}

.hero-content {
  max-width: 850px;
  animation: fadeUp 1.5s ease;
}

@keyframes fadeUp {
  from {
    opacity: 0;
    transform: translateY(40px);
  }

  to {
    opacity: 1;
    transform: translateY(0);
  }
}

.small-text {
  color: #ffc7e8;
  letter-spacing: 5px;
  text-transform: uppercase;
  font-size: .8rem;
  margin-bottom: 20px;
}

.big-heart {
  font-size: 90px;
  display: inline-block;
  filter: drop-shadow(0 0 25px #ff2998);
  animation: heartbeat 1.3s infinite;
}

@keyframes heartbeat {
  0%, 100% {
    transform: scale(1);
  }

  50% {
    transform: scale(1.18);
  }
}

h1 {
  margin-top: 15px;
  font-size: clamp(3rem, 10vw, 7rem);
  line-height: .95;
  color: #ff9bd5;
  text-shadow:
    0 0 10px #ff2998,
    0 0 30px #ff2998,
    0 0 70px rgba(255, 41, 152, .6);
}

.subtitle {
  margin: 25px auto;
  max-width: 650px;
  font-size: 1.2rem;
  line-height: 1.8;
  color: #ffe5f3;
}

.date {
  display: inline-block;
  margin-top: 10px;
  padding: 10px 22px;
  border: 1px solid rgba(255,255,255,.25);
  border-radius: 30px;
  background: rgba(255,255,255,.07);
  backdrop-filter: blur(10px);
  color: #ffd4eb;
}

/* =========================
   BUTTON
========================= */

button {
  border: none;
  cursor: pointer;
  color: white;
  font-weight: bold;
  font-size: 1rem;
}

.main-button {
  margin-top: 35px;
  padding: 17px 35px;
  border-radius: 50px;
  background: linear-gradient(45deg, #ff168d, #ff72c5);
  box-shadow:
    0 0 20px rgba(255, 30, 145, .5),
    0 0 50px rgba(255, 30, 145, .2);
  transition: .3s;
}

.main-button:hover {
  transform: translateY(-4px) scale(1.05);
  box-shadow:
    0 0 30px rgba(255, 30, 145, .8),
    0 0 70px rgba(255, 30, 145, .4);
}

/* =========================
   SECTIONS
========================= */

.section {
  position: relative;
  z-index: 3;
  padding: 100px 20px;
  text-align: center;
}

.section-title {
  font-size: clamp(2rem, 6vw, 3.5rem);
  color: #ff9bd5;
  text-shadow: 0 0 20px rgba(255, 30, 145, .5);
  margin-bottom: 20px;
}

.section-subtitle {
  max-width: 650px;
  margin: auto auto 45px;
  color: #ffdcec;
  line-height: 1.7;
}

/* =========================
   LOVE LETTER
========================= */

.letter {
  max-width: 750px;
  margin: auto;
  padding: 45px;
  border-radius: 30px;

  background: linear-gradient(
    145deg,
    rgba(255,255,255,.12),
    rgba(255,255,255,.04)
  );

  border: 1px solid rgba(255,255,255,.15);

  box-shadow:
    0 20px 80px rgba(0,0,0,.35),
    inset 0 0 30px rgba(255,255,255,.03);

  backdrop-filter: blur(15px);
  text-align: left;
}

.letter p {
  color: #ffe9f5;
  font-size: 1.1rem;
  line-height: 2;
  margin-bottom: 20px;
}

.signature {
  color: #ff9bd5 !important;
  font-size: 1.3rem !important;
  text-align: right;
}

/* =========================
   PHOTOS
========================= */

.photo-grid {
  max-width: 1000px;
  margin: auto;
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 20px;
}

.photo {
  height: 350px;
  border-radius: 25px;
  overflow: hidden;
  position: relative;
  border: 1px solid rgba(255,255,255,.15);
  box-shadow: 0 15px 40px rgba(0,0,0,.3);
  transition: .5s;
}

.photo:hover {
  transform: translateY(-10px) rotate(1deg);
}

.photo img {
  width: 100%;
  height: 100%;
  object-fit: cover;
  transition: .5s;
}

.photo:hover img {
  transform: scale(1.08);
}

.photo-label {
  position: absolute;
  bottom: 0;
  left: 0;
  right: 0;
  padding: 30px 15px 15px;

  background: linear-gradient(
    transparent,
    rgba(0,0,0,.8)
  );

  color: white;
}

/* =========================
   MEMORIES
========================= */

.memory-container {
  max-width: 850px;
  margin: auto;
}

.memory {
  display: flex;
  gap: 25px;
  margin: 35px 0;
  text-align: left;
  align-items: center;
}

.memory-number {
  min-width: 65px;
  height: 65px;
  display: flex;
  justify-content: center;
  align-items: center;

  border-radius: 50%;
  background: linear-gradient(45deg, #ff218f, #ff8bd0);
  box-shadow: 0 0 25px rgba(255, 33, 143, .5);

  font-size: 1.4rem;
  font-weight: bold;
}

.memory h3 {
  color: #ffb0db;
  margin-bottom: 8px;
}

.memory p {
  color: #ead4e2;
  line-height: 1.6;
}

/* =========================
   GIFT
========================= */

.gift-area {
  margin-top: 40px;
}

.gift {
  font-size: 100px;
  cursor: pointer;
  display: inline-block;
  animation: giftFloat 2s infinite ease-in-out;
  filter: drop-shadow(0 0 25px #ff2998);
  transition: .3s;
}

.gift:hover {
  transform: scale(1.15);
}

@keyframes giftFloat {
  0%, 100% {
    transform: translateY(0);
  }

  50% {
    transform: translateY(-15px);
  }
}

.surprise {
  display: none;
  max-width: 700px;
  margin: 35px auto 0;
  padding: 35px;
  border-radius: 25px;
  background: rgba(255,255,255,.08);
  border: 1px solid rgba(255,255,255,.15);
  backdrop-filter: blur(12px);
  animation: reveal .8s ease;
}

@keyframes reveal {
  from {
    opacity: 0;
    transform: scale(.8);
  }

  to {
    opacity: 1;
    transform: scale(1);
  }
}

.surprise h2 {
  color: #ff9bd5;
  font-size: 2.5rem;
  margin-bottom: 20px;
}

.surprise p {
  color: #ffe5f2;
  font-size: 1.2rem;
  line-height: 1.8;
}

/* =========================
   FINAL
========================= */

.final {
  min-height: 80vh;
  display: flex;
  justify-content: center;
  align-items: center;
}

.final-content {
  max-width: 800px;
}

.final-heart {
  font-size: 110px;
  animation: heartbeat 1.2s infinite;
  filter: drop-shadow(0 0 30px #ff2998);
}

.final h2 {
  font-size: clamp(2.5rem, 8vw, 5rem);
  color: #ff9bd5;
  text-shadow: 0 0 30px #ff2998;
  margin: 20px 0;
}

.final p {
  font-size: 1.2rem;
  line-height: 1.9;
  color: #ffe5f3;
}

/* =========================
   MUSIC
========================= */

/* =========================
   CONFETTI
========================= */

.confetti {
  position: fixed;
  width: 10px;
  height: 10px;
  z-index: 50;
  pointer-events: none;
  animation: confettiFall 4s linear forwards;
}

@keyframes confettiFall {
  from {
    transform: translateY(-20px) rotate(0deg);
    opacity: 1;
  }

  to {
    transform: translateY(110vh) rotate(720deg);
    opacity: 0;
  }
}

/* =========================
   RESPONSIVE
========================= */

@media (max-width: 700px) {

  .hero {
    min-height: 100svh;
  }

  .big-heart {
    font-size: 70px;
  }

  .subtitle {
    font-size: 1rem;
  }

  .letter {
    padding: 28px 22px;
  }

  .photo-grid {
    grid-template-columns: 1fr;
  }

  .photo {
    height: 400px;
  }

  .memory {
    align-items: flex-start;
  }

  .memory-number {
    min-width: 50px;
    width: 50px;
    height: 50px;
    font-size: 1rem;
  }

  .section {
    padding: 75px 18px;
  }
}
</style>
</head>

<body>

<!-- =========================
     BACKGROUND
========================= -->

<div class="stars" id="stars"></div>
<div class="hearts" id="hearts"></div>

<!-- =========================
     HERO
========================= -->

<section class="hero">

  <div class="hero-content">

    <div class="small-text">
      A little surprise for someone very special
    </div>

    <div class="big-heart">
      ❤️
    </div>

    <h1>
      Happy Birthday
      <br>
      PAPAAAAAA💕
    </h1>

    <div class="date">
      🎂 18/08/2026 🎂 
    </div>

    <br>

    <button class="main-button" onclick="scrollToLetter()">
      💌 I Have Something For You
    </button>

  </div>

</section>


<!-- =========================
     LOVE LETTER
========================= -->

<section class="section" id="letter">

  <h2 class="section-title">
    A Letter For You 💌
  </h2>
  <div class="letter">

    <p>
      My love.....
    </p>

    <p>
      Happieeee Birthdayyyy kallipennee ❤️
    </p>

    <p>
      I don't know if words will ever be enough to explain
      how much you mean to me. But I want you to know that
      having you in my life is one of the greatest things
      that has ever happened to me......
    </p>

    <p>
      Thank you for all the little moments 🫂  ,even the silly
      moments that somehow become my favorite memories.
    </p>

    <p>
      Happy Birthday, sotheeee 🥰
      <br>
      I love you more than words can say.😘
    </p>

    <p class="signature">
      Forever yours ❤️
    </p>

  </div>

</section>


<!-- =========================
     MEMORIES
========================= -->
<!-- =========================
     MEMORIES TIMELINE
========================= -->

<section class="section">

  <h2 class="section-title">
    Just A Few Reasons ❤️
  </h2>

  <p class="section-subtitle">
    Maahh ,there are a million reasons why I love you...
    Here are just a few;
  </p>

  <div class="memory-container">

    <div class="memory">

      <div class="memory-number">
        ❤🍅
      </div>

      <div>
        <h3>Thakkalimookuu🍅</h3>
      </div>

    </div>


    <div class="memory">

      <div class="memory-number">
        ✨
      </div>

      <div>
        <h3>The way you look at me...</h3>
      </div>

    </div>


    <div class="memory">

      <div class="memory-number">
        🥰
      </div>

      <div>
        <h3>The way you indirectly control me..</h3></h3>


      </div>

    </div>

  </div>

</section>


<!-- =========================
     GIFT
========================= -->

<section class="section">

  <h2 class="section-title">
    There's One More Thing... 🎁
  </h2>

  <p class="section-subtitle">
    I saved the most important message for last.
  </p>

  <div class="gift-area">

    <div class="gift" onclick="openGift()">
      🎁
    </div>

    <p style="color:#ffd6eb;margin-top:15px;">
      Tap the gift ❤️
    </p>

    <div class="surprise" id="surprise">

      <h2>
        ✨ Surprise! ✨
      </h2>

      <p>

        Kilometers may keep us apart, but my heart is always with you. ❤️
        I wish I could hold you close and celebrate this day by your side.
        Until then, let my love cross every distance between us.
        You are my favorite thought, my sweetest feeling, and my happiest place....

        someday, we’ll celebrate together. 🎂💕
      </p>
      
      <p style="margin-top:20px;">
        Happy Birthday,maaahhh 🎂💕
      </p>

    </div>

  </div>

</section>


<!-- =========================
     FINAL MESSAGE
========================= -->

<section class="section final">

  <div class="final-content">

    <div class="final-heart">
      ❤️
    </div>

    <h2>
      I Lubb youhhh 😘😘
    </h2>

    <p>
      Today, tomorrow, and all the days that come after.
      <br>
      Here's to you, to us, and to all the memories
      we haven't made yet....🫂💕
    </p>

    <button class="main-button" onclick="celebrate()">
      🎉 One Last Surprise
    </button>

  </div>

</section>


<!-- MUSIC BUTTON -->

<button class="music-button" onclick="toggleMusic()" id="musicButton">
  🎵
</button>

<audio id="music" loop>
  <source src="song.mp3" type="audio/mpeg">
</audio>


<script>

/* =========================
   CREATE STARS
========================= */

const stars = document.getElementById("stars");

for (let i = 0; i < 100; i++) {

  const star = document.createElement("div");

  star.className = "star";

  star.style.left = Math.random() * 100 + "vw";
  star.style.top = Math.random() * 100 + "vh";

  star.style.animationDelay =
    Math.random() * 3 + "s";

  stars.appendChild(star);
}


/* =========================
   FLOATING HEARTS
========================= */

const hearts = document.getElementById("hearts");

const heartTypes = [
  "❤️",
  "💖",
  "💕",
  "💗",
  "💓",
  "✨"
];

for (let i = 0; i < 25; i++) {

  const heart = document.createElement("div");

  heart.className = "floating-heart";

  heart.innerHTML =
    heartTypes[
      Math.floor(Math.random() * heartTypes.length)
    ];

  heart.style.left =
    Math.random() * 100 + "vw";

  heart.style.fontSize =
    15 + Math.random() * 25 + "px";

  heart.style.animationDuration =
    6 + Math.random() * 8 + "s";

  heart.style.animationDelay =
    Math.random() * 8 + "s";

  hearts.appendChild(heart);
}


/* =========================
   SCROLL TO LETTER
========================= */

function scrollToLetter() {

  document
    .getElementById("letter")
    .scrollIntoView({
      behavior: "smooth"
    });

}


/* =========================
   OPEN GIFT
========================= */

function openGift() {

  const surprise =
    document.getElementById("surprise");

  surprise.style.display = "block";

  createConfetti(100);

  setTimeout(() => {

    surprise.scrollIntoView({
      behavior: "smooth",
      block: "center"
    });

  }, 300);

}


/* =========================
   CONFETTI
========================= */

function createConfetti(amount) {

  const emojis = [
    "❤️",
    "💖",
    "💕",
    "✨",
    "🎉",
    "🎊",
    "💗"
  ];

  for (let i = 0; i < amount; i++) {

    const piece =
      document.createElement("div");

    piece.className = "confetti";

    piece.innerHTML =
      emojis[
        Math.floor(Math.random() * emojis.length)
      ];

    piece.style.left =
      Math.random() * 100 + "vw";

    piece.style.top =
      "-30px";

    piece.style.fontSize =
      12 + Math.random() * 25 + "px";

    piece.style.animationDuration =
      2 + Math.random() * 3 + "s";

    piece.style.animationDelay =
      Math.random() * 1 + "s";

    document.body.appendChild(piece);

    setTimeout(() => {
      piece.remove();
    }, 5000);

  }
}


/* =========================
   FINAL CELEBRATION
========================= */

function celebrate() {

  createConfetti(200);

  for (let i = 0; i < 50; i++) {

    const heart =
      document.createElement("div");

    heart.innerHTML =
      heartTypes[
        Math.floor(Math.random() * heartTypes.length)
      ];

    heart.style.position = "fixed";
    heart.style.left =
      Math.random() * 100 + "vw";

    heart.style.top =
      Math.random() * 100 + "vh";

    heart.style.fontSize =
      20 + Math.random() * 40 + "px";

    heart.style.zIndex = "100";

    heart.style.pointerEvents = "none";

    heart.style.animation =
      "floatHeart 4s linear forwards";

    document.body.appendChild(heart);

    setTimeout(() => {
      heart.remove();
    }, 4000);

  }

}


/* =========================
   MUSIC
========================= */

let musicPlaying = false;

function toggleMusic() {

  const music =
    document.getElementById("music");

  const button =
    document.getElementById("musicButton");

  if (!musicPlaying) {

    music.play().catch(() => {
      alert("Add your song as song.mp3 in the same folder.");
    });

    button.innerHTML = "🔊";
    musicPlaying = true;

  } else {

    music.pause();

    button.innerHTML = "🎵";
    musicPlaying = false;

  }

}

</script>

</body>
</html>